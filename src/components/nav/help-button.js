import React from 'react'
import { Button } from 'react-bootstrap'

function HelpButton() {
    return (
        <div>
            <Button id="help-button" className="help-button">Help</Button>
        </div>
    )
}

export {
    HelpButton
}